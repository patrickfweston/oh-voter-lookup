services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: voter
      POSTGRES_PASSWORD: voter
      POSTGRES_DB: voter_guide
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
